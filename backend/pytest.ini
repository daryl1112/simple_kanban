[pytest]
# Test discovery configuration. Tests live under ./tests and mirror the
# app/ package structure (see README).
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
