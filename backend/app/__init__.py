"""DevBoard backend application package.

A FastAPI service that exposes every board operation (projects, cards,
dependencies, comments, assignment) as a REST API. The same API backs the
Next.js UI, so any action can be performed manually or programmatically.
"""

__version__ = "1.0.0"
